{
  "name": "@engine/wasm-modules",
  "version": "0.1.0",
  "description": "AssemblyScript compute kernels for @engine/core — pathfinding, noise, RNG, and flood-fill — compiled to standalone .wasm with a tiny raw export surface (numbers + linear memory, no runtime). Ships only the compiled artifacts; the JS wrappers that instantiate and call them (Pathfinder, NoiseGenerator) live in @engine/core/wasm.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    "./pathfinding.wasm": "./dist/pathfinding.wasm",
    "./noise.wasm": "./dist/noise.wasm",
    "./rng.wasm": "./dist/rng.wasm",
    "./floodfill.wasm": "./dist/floodfill.wasm"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "node build/compile.mjs",
    "prepack": "npm run build",
    "typecheck": "asc --noEmit src/pathfinding.ts --config asconfig.json"
  },
  "devDependencies": {
    "assemblyscript": "0.28.17"
  }
}
