---
summary: The current-state snapshot: one terse line per brief, architecture milestones, current sim/determinism behaviour, and open gaps. The single source for brief state.
updated: 2026-07-15
---

# Project Status

Current-state **snapshot** (2026-07-11). Terse one-liner per brief here; full implementation detail lives in each brief file ([briefs/](../briefs/)), recent passes in [log.md](../log.md) (older entries trimmed → git history), live design gaps in [open-questions.md](open-questions.md).

> **2026-07-15 Engine brief [21](../briefs/engine/todo/21-turborepo-task-runner.md) filed (todo, not built) — Turborepo task runner.** Baseline measured: root typecheck **44s serial** over 14 workspaces, first-failure-stops (the 2026-07-09 gate-rot mechanism). Turbo layers on npm workspaces (locked decision untouched); packages are build-less "JIT packages" so the win is parallel + cached typecheck/test. Load-bearing scope item: declare cross-package task `inputs` (known trap: `farmer-frames.test.ts` reads the Farm client atlas with no dependency edge); determinism runs never cache-served.

> **2026-07-15 Structure-survey batch filed (todo, not built): game [115](../briefs/game/todo/115-farm-client-net-rename-and-tool-hygiene.md) (Farm client `worker/`→`net/` + `Worker*`→`Sim*` protocol renames + run-sim probe grouping), game [116](../briefs/game/todo/116-citadel-sim-runner-split.md) (citadel-sim 1,196-line `index.ts` → run-sim-shaped modules, byte-identical-stdout gate), engine [20](../briefs/engine/todo/20-engine-ghost-subsystems.md) (delete the consumer-less `Animator`/`Clip` ghost, adjudicate single-consumer `@engine/core/assets`).** All mechanical/behavior-preserving; survey details in [log.md](../log.md). `engine/todo/` is no longer empty.

> **2026-07-15 Brief [114](../briefs/game/todo/114-citadel-client-main-decomposition.md) filed (todo, not built) — Citadel client `main.ts` decomposition.** A project-structure survey found `main.ts` at **1,949 lines** (largest source file in the repo), against the module-directory convention Farm's client already follows (`src/main/`). Spec: behavior-preserving split along the file's own banner seams, hazards named (boot-gap guard, single `newEventsSince` pass feeding toasts+audio, shared mutable state, Vite entry), plus the workspace-map doc drift (`@engine/ui` absent from root CLAUDE.md + architecture.md). `game/todo/` is no longer empty.

> **2026-07-15 Both `todo/` queues cleared — the last three open briefs closed unbuilt.** [101](../briefs/game/superseded/101-farm-perishability-distance-pricing.md) (Farm perishability/distance pricing) and [107](../briefs/game/superseded/107-farm-visual-verification-session.md) (visual verification session) → **superseded, closed unbuilt** (Farm is in maintenance; specs retained in `todos/` and the source briefs). [96](../briefs/game/superseded/96-citadel-building-art-style-reference.md) (Citadel building art-style) → **superseded as not-a-task** — it was always a living reference mis-filed in `todo/`; fold its reference-asset section into [citadel-art-style.md](citadel-art-style.md). `engine/todo/` and `game/todo/` are now both empty.

> **2026-07-15 Engine audio subsystem (`33f9a38`, engine brief 19 — DONE, code-only; browser sign-off owed).** New generic `@engine/core/audio` (`AudioEngine`), a **client/render subsystem strictly off the deterministic sim path** — same layer as particles/toasts/juice, `sim-core` untouched. Signal chain per-voice source → per-voice gain → **master gain** → destination; `muted`/`volume` gate at master; voice cap (16, skips when saturated); voices reaped on `onended` + a scheduled-end backstop. Browsers start the `AudioContext` **suspended** → engine exposes `unlock()` (clients call on first gesture); **pre-unlock `play()` is a safe no-op returning false** (no throw, no node, no autoplay-gate error). Headless-testable via an injected `AudioContextLike` factory; degrades to a **silent stub** where Web Audio is absent (node/jsdom). **v1 sounds are procedural synth — zero committed binary assets** (a `buffer` `SoundSpec` for real assets is built but wired to no `.wav`). Proven end-to-end with **3 test sounds per game**: Farm's `FarmAudio` is an injected `JuiceAudioSink` off `JuiceLayer`'s existing new-event pass (inherits the resync-skip guarantee); Citadel's `CitadelAudio` keys off `toast.ts`'s `toneOf`, fed from the same `newEventsSince` loop as toasts + a "Mute sound" checkbox in the settings modal. The brief's original Farm hook named a `lastEventCount` cursor that no longer exists (brief 97 replaced it with a tick high-water mark) — corrected before dispatch. Built via `plan-split-dispatch`: Wave 1 Chunk A (opus, engine) → Wave 2 Chunks B+C (Sonnet, parallel client wiring). Gates: typecheck 0; full suite **2275 green** (+34 audio tests); **Farm `CHECK_DETERMINISM` + Citadel headless run both byte-identical** (the proof audio never reaches the sim). **Owed:** a real-browser audio sign-off (a code-only session can't hear it) — including whether Citadel's every-warn/info tick is too frequent. Details: [architecture.md](architecture.md) "Audio".

> **2026-07-14 The in-canvas UI gets a real font + an icon set (engine brief 18 / the [authored-typography todo](../todos/closed/2026-06-30-engine-ui-authored-typography-and-icons.md) — DONE).** `@engine/ui`'s hand-coded **5×7 ASCII bitmap font** (which read as a debug font, and whose ASCII-only limit is why Citadel's build bar had been downgraded from an emoji icon grid to wide text labels) is replaced by **UNSCII** (public domain, `.hex` vendored + converted once into committed glyph tables): `BODY_FONT` 8×8 + `DISPLAY_FONT` 8×16, still a deterministic boot-time atlas bake with zero runtime font asset. Plus a new **icon pipeline**: 34 icons at 16×16 stored as **shade-index masks** (never colours) with the consumer supplying a 3-colour ramp — the one design that lets a single icon set serve Citadel (Apollo-46) and Farm (EDG32) while the engine imports neither. Citadel's build bar is a **compact icon grid** again and its goods strip is iconified. **The migration's real cost was reflow** (5px → 8px glyphs): four layout bugs, *none* caught by a unit test, all needing a browser — see [engine-ui.md](engine-ui.md) "the reflow trap". `label({ maxWidth })` (wrapping) is new, because labels physically could not wrap before. Gates: typecheck 0; full suite **2241 green**; both games browser-verified on a real GPU. Details + the authoring rule: [engine-ui.md](engine-ui.md).

> **2026-07-14 Citadel building art rebuilt from scratch as 3D meshes (`6cc32fb` Phase 1 + `d1e7c7c` Phase 2) — browser-approved.** The 2D ASCII pixel-recipe approach (`iso-draw.ts`) couldn't produce recognizable complex buildings reliably (blind generation, no visual loop — Wave 5's forms failed unseen). Replaced with an **in-code 3D-mesh renderer** (user's idea, research-backed — how pros make iso art): an [indexed triangle mesh](../../games/citadel/client/src/render/sprites/mesh/) (positions + tris + per-face material) built from parametric primitive generators (box/cylinder/cone/pyramid/gable + transforms + merge), rendered by a small software rasterizer — dimetric projection matching `iso.ts`, back-face cull, per-pixel **z-buffer**, flat-shade each triangle by face normal onto 3 adjacent **Apollo** ramp steps, silhouette/depth-crease outline. Emits the existing `RasterizedRecipe` contract so `atlas.ts` drops it in as per-name frame overrides (`MESH_OVERRIDES`, now covering all 21 + the mill's 8 animation frames). All 21 buildings modeled (crenellated keep/tower, steepled chapel, real windmill with 4 animated sail blades, open-stall market, terraced-pit quarry, open-forge smith, furrowed farm, raised-cabin watchpost, …); heights match `BUILDING_HEIGHT_TILES` so tall buildings loom. Built via `plan-split-dispatch` (one opus builder across phases; iterated on browser screenshots — the mill "still old" catch was the animation frames needing override too). Gates: typecheck 0; Apollo palette guard 8/8; @citadel/client 490/490. **Phase 3 DONE 2026-07-14 (`dfd754d`, [closed todo](../todos/closed/2026-07-14-citadel-mesh-phase3-cleanup.md)):** the `@lit` night frames are meshes too (new **emissive** material path + real window geometry: dark pane by day, lamplight at night; anti-drift — the lit frame calls the same day factory and remaps materials), the char-recipe building path is **deleted** (`iso-draw.ts` + `BUILDING_RECIPES` + `roof.test.ts`, **−1,964 lines**), and the silhouette/recipe guards now grade `MESH_OVERRIDES` (the rasters the atlas bakes) instead of char art nothing drew. `BUILDING_SPRITE_TYPES` derives from `MESH_MODELS`. Full suite 1376 green; real-GPU verified. Rules for touching the pipeline: [citadel-rendering.md](citadel-rendering.md). Full spec: [closed todo](../todos/closed/2026-07-13-citadel-3d-box-asset-renderer.md).

> **2026-07-13 Citadel moved to the Apollo-46 palette (`83efacc`) — Farm + engine stay EDG32; the palette guard is now per-scope.** User call ("do apollo for citadel") acting on the [palette-evaluation todo](../todos/closed/2026-07-13-citadel-palette-evaluation.md): EDG32's gamut has no desaturated olive-grey midtones (blue-tinted greys, rusty mid-browns) — a poor fit for muted medieval naturals, and colour is the axis the day/night wash degrades. **Decision #28** ([citadel-decisions.md](citadel-decisions.md)). The migration was an **import redirect, not a call-site rewrite**: every Citadel colour already routed through named `EDG.*` role constants (zero raw hex), so a new [citadel-palette.ts](../../games/citadel/client/src/render/citadel-palette.ts) exports `CITADEL_PAL` (the same 32 role keys remapped to Apollo hex, luminance ordering preserved per ramp) and Citadel files import it as `CITADEL_PAL as EDG` — ~200 usage sites untouched. Shared `@engine/ui` chrome is re-skinned via an injected Citadel Apollo `Theme` ([ui/citadel-theme.ts](../../games/citadel/client/src/ui/citadel-theme.ts)); engine defaults stay EDG32 so Farm is unaffected. The guard ([palette.test.ts](../../engine/core/src/render/palette.test.ts)) is **per-scope** (`games/citadel/`→Apollo, else EDG32); since the engine never imports a game, the 46 Apollo swatches are inlined engine-side and pinned to the module by a colocated Citadel test. Gates: typecheck 0; **@engine/core 186/186** (Farm still EDG32-clean under the guard); **@citadel/client 490/490** (render/UI test expectations updated to the Apollo source of truth, no matcher weakened). **First-pass colour choices** — a `?showcase` browser eyeball to tune them is the natural (deferred) follow-up; the objective gates already pass. Four chunks (opus foundation + role-mapping, 3 Sonnet: art redirect, UI+theme, test expectations).

> **2026-07-13 Wave 5 DONE — the 8 look-alike box-buildings get distinct silhouettes (`0d6c1b3`); the [remaining-work dispatch plan](../todos/closed/2026-07-10-remaining-work-dispatch-plan.md) is now COMPLETE (all waves 1–5 landed).** 8 of 21 `BUILDING_RECIPES` were the same ~128×92 box with a different roof colour — colour being the axis the day/night wash degrades, so at dusk a bakery and a house converged, contradicting `buildings.ts`'s own silhouette-first goal. Each now reads with colour stripped: new `iso-draw` form-styles (`cottageStyle` `"cabin"` for a reduced-footprint woodcutter, `"forge"` for an open-sided smith canopy on posts + back hearth), a hipped roof + larger domed oven bulge + smoke plume for bakery, and a stilted raised-platform watchpost that no longer reads as a house. `house` kept as the plain reference; `market`/`public-square`/`quarry` already read distinctly (alpha-mask evidence) and were left to avoid regression. All EDG32, within the 128×92 frame (**zero atlas growth**). New `buildings-silhouette.test.ts` proves all 8 pairwise-distinguishable on the bottom-anchored alpha-mask metric (min house~bakery=19, floor 12, RED-verified). Stale `4×`→`2×` comment fixed. Gates: typecheck 0; **EDG32 palette guard 6/6**; **@citadel/client 485/485**. Two junior/opus chunks (opus for the pixel-art primitives, Sonnet for the independent test). Per the user's call the **`?showcase` browser verification was skipped** at closeout (the RED-verified silhouette test is the objective gate; the "reads at a glance / looks good" eyeball is deferred). **Open follow-up:** a [Citadel palette re-evaluation todo](../todos/closed/2026-07-13-citadel-palette-evaluation.md) was filed — EDG32's gamut gap (no desaturated olive-grey midtones) is a known weak fit for muted medieval naturals.

> **2026-07-13 Wave 4 DONE — Challenge mode closed out (brief 103, `c2caecc`); the dispatch plan's Wave 4 is complete.** Scope 1 (the mode plumbing + in-canvas cozy/challenge picker) was found already landed in code (`658bbeb`/`f65112d`) and correct — all four challenge flags (`cozyThreats`/`seedTown`/`deferThreatsUntilBuildings`/`enableArmy`) round-trip through `serializeSave`/`loadFromSave` — so scope 1 only needed the missing **test coverage**: a worker `mode→flags` mapping test + `enableArmy`/`deferThreatsUntilBuildings` save/load round-trips. Scope 2's headline "what rotted" (the inert `sack` fixture) was **already fixed** 2026-07-11; what remained was three **dead decree branches** reading an `activeDecrees` set nothing writes since the Phase-G purge. Per **decision #27** they were **re-pointed to autonomous `cozyThreats:false`-gated behaviors** (conscription during a sharp raid; rationing only in sharp famine; the tithe→relief-reserve cushion, now **bread-only** — the cushion only draws bread, so taxing tools was a purposeless drag that also eroded `army.test.ts`'s tool counts, the collision that surfaced the design call). Cozy is byte-identical **by construction** (the branches never run when `cozy=true`) and the existing cozy guards confirm it. Falsified decree tests rewritten to the new truth (un-weakened; cozy negatives kept as byte-identity guards). Pure integer arithmetic — **no new RNG**. Gates: typecheck 0; **@citadel/sim-core 309/309**, **@citadel/client 483/483** (a pre-existing `@engine/ui` symlink gap that broke client test-loading was fixed via `npm install`). The determinism ×3 + real-browser `playtest-citadel` acceptance gates were **consciously skipped at closeout** (user call): no RNG change ⇒ reproducibility unaffected, cozy byte-identical by construction, and `sharp-raid-path.test.ts` already proves the challenge start→Town→keep→raid→sack chain is reachable. **Next per the [dispatch plan](../todos/closed/2026-07-10-remaining-work-dispatch-plan.md): Wave 5** (building silhouette differentiation, render-only).

> **2026-07-13 Wave 3.5 DONE — the pop-6-7 deadlock breaks; the blocker was worker ALLOCATION, not immigration (`bbca1e9`).** The P1 diagnosis below (an "immigration hard-stop at zero bread surplus") was **wrong a second time** — a per-day census of the headless deadlock drip shows **25 population increases**: arrivals come freely on the surplus/break-even days. The town pinned at pop 6-7 because of allocation: **(1)** `removeOneVillager` dropped the **NEWEST** villager on starvation (LIFO), so every settler sent to staff the idle second bakery was the one starvation culled next — growth was always reversed; the starvation path now drops a **redundant** worker (one on a glutted-output producer) via `{ preferRedundant: true }` (disease/raids still take the newest) — **this alone reaches Town** (day 99). **(2)** assignment staffed the second farm/mill (gluttng grain/flour 500+) before the second bakery; a **glut-skip** steers a second worker past an abundant-output producer (≥8 days supply) to the scarce bottleneck, bounded to already-staffed types with a no-skip fallback — accelerates to **Town day 53**. **(3)** the immigration **trickle floor** (what this todo originally scoped) is kept — byte-identical on every baseline, covers the clean single-bakery break-even case — but is **NOT** what breaks the deadlock. The drip that was pinned at pop 7/Village **forever** now reaches **Town (peak)**, so the downside rule (#9) holds. Gates: typecheck 0; full repo tests 0 (citadel sim-core **301**, +15 new: `immigration.test` + `deadlock-allocation.test`); Citadel determinism **MATCH ×3 seeds**; grow/sack baselines drift **only in grain counts** (pop/bread/happiness/tier/outcomes identical); `starve` still `gameOver=true`. **Deferred P2:** the town reaches Town then **oscillates back to Village** (bread break-even, grain still gluts) — the cadence imbalance (farms≫mills≫bakeries) + happiness throttle, a balance pass, not this fix. [Closed todo](../todos/closed/2026-07-11-citadel-solo-town-tier-unreachable.md).

> **2026-07-11 CORRECTION + Wave 3.5 scoped — Town IS reachable solo (day 28, keep day 59); the real P1 is an unrecoverable immigration deadlock.** The Wave-3 entry below (and brief 113's closeout caveat) claim "solo cozy cannot reach Town tier" — **that root-cause narrative was wrong**, produced by three playtest-harness bugs (day-0 over-building; woodcutters never sited on Forest so every one was silently rejected — wood income was zero because there *were no woodcutters*; the planner siting buildings on its own road carpet). Headless diagnostics on the client's exact bootstrap flags proved a drip-built town reaches **Town day 28, keep day 59**, then lives 250+ days at pop 11–14 under live cozy raids. **What survives, corrected and still P1** ([rewritten todo](../todos/closed/2026-07-11-citadel-solo-town-tier-unreachable.md)): the pop 6–7 equilibrium is an **unrecoverable attractor** — immigration hard-stops at zero bread surplus, one bakery feeds exactly ~6–7, and escaping needs the arrival the gate blocks; a town that misses the early seeded-bread window stays locked **forever** (proven: 250 days, 23 buildings placed, 155 wood banked, grain 510, bread 0, six buildings staffed). That hard stop violates the downside rule (#9: throttle, never halt). **Wave 3.5** (user-approved): immigration gets a trickle floor — blocked-surplus towns with housing + a connected unstaffed building and no active starvation still receive one settler per N days (deterministic day-count, no new RNG); `starve` must stay `gameOver=true`; the deadlock fixture must recover. Secondary P2 noted for later: farm≫mill≫bakery cadence lets grain pile 500+ while bread reads 0.

> **2026-07-11 Wave 3 DONE — briefs 102 + 113 (`c22145e`) — and the playtest that verified them found a P1.** Three parallel junior/Sonnet chunks after a controller design gate that had to **correct 113's premise against the code**: the raid body (spawn→march→spatial garrison interception→cozy pilfer→snapshot→client interp render) has existed since `af31818` (2026-06-26) — the grilling session specced a system that was already built, and the only real gap was that a cozy raider **vanished at the keep**. It now departs: `RaiderState.leaving` retraces the walked path to the spawn edge, arrival effects and event strings byte-unchanged, sharp path byte-identical (`sack` still PASS day 50). **102** landed the settled smallest set: well coverage multiplies disease onset (`×(1−0.5·coveredFraction)`, IEEE754-exact no-op at zero wells, draw-count proven constant), healer named in outbreak start/end copy, well/healer inspect copy states both mechanics. Gates: typecheck 0; tests exit 0 (sim-core 297, client 474); Citadel determinism MATCH ×3; **all six scenarios byte-identical to pre-wave HEAD except one by-design text-only string** (sack's fixture has a healer). **Browser-verified on real WebGPU**: raid seen marching ~17 days in, "found little worth taking and left" at distance 2, then the distance series retraces 2→113 over 16 days to an edge despawn; well/healer copy live; a live cozy outbreak on-screen. **⚠️ The verification had to route through `?mp` (town-hall raid anchor, free placement), because three live solo attempts (290/300/536 in-game days, three strategies) all proved [solo cozy CANNOT reach Town tier](../todos/closed/2026-07-11-citadel-solo-town-tier-unreachable.md)** — pop equilibrates at 7–9 (services steal bread-chain workers), wood income collapses to ~0, and the `sack` scenario only earns Town via its 5-wood/day injection. Keep/garrison/raids are unreachable content in solo, and **brief 103's live acceptance is gated on this P1**, not on anything 103 itself contains. The 2026-06-22 playtest-findings todo is fully closed (P3 was its last item).

> **2026-07-11 Wave 2.5 DONE — both headless runners emit an LLM-readable JSON run report (`d224b09`).** The [headless-JSON todo](../todos/closed/2026-07-11-headless-json-run-for-both-games.md)'s read-only scope, run as two parallel junior/Sonnet chunks on disjoint lanes. A generic **`RunReport<TDay,TEnd>` envelope in `@engine/core/sim`** (`run-report.ts`: meta + compact per-day timeline + cumulative event log `{total,missed,entries,truncated}` + full end-state once + `outcome`; **no timestamps by contract**, so a report is byte-identical per seed) behind symmetric **`REPORT=1` / `REPORT_FILE=`** flags on `npm run sim` and `npm run sim:citadel`. The two event feeds needed two collectors: Farm's `EventFeedSystem.recent()` is a capped tail with per-entry ticks → per-tick **high-water harvest** (`missed` provably 0); Citadel's `recentEvents` is a capped 20-tail of plain strings → every-tick sampling (report-gated, zero extra snapshots otherwise) with gaps **accounted against the monotonic `eventsSeq`**. Citadel's fire/disease comparison scenarios report both halves in `endState`; `sack`'s exit-1-on-failure is preserved. Reporting is observer-only — no new RNG draws, no sim inputs. The folded-in `play.mjs` driver fix was found **already landed** (`__citadel.snapshot()` + snapshot-based `readHud`, DOM scrape only a fallback). **Deferred deliberately:** the scripted-action layer. Gates: typecheck 0; full repo tests exit 0; Farm determinism **MATCH ×3**; Citadel double-run stdout **byte-identical ×3 seeds** and byte-identical to pre-change code on grow+sack; narratability eyeballed on a real `starve` report. Next per the [dispatch plan](../todos/closed/2026-07-10-remaining-work-dispatch-plan.md): **Wave 3** (102 disease counterplay + 113 raid body) — 103's `sack` blocker already cleared 2026-07-11.

> **2026-07-11 The `sack` fixture was INERT, not drifting — and three green tests said otherwise (`7c76522`, `36382d2`).** Chased before Wave 3 because [brief 113](../briefs/game/done/113-citadel-raid-gets-a-body.md) rebuilds the raid on this machinery. **Four defects:** (1) `cozyThreats` defaults **true**, and a cozy raid pilfers-and-leaves — it *cannot sack*; `siege`/`sack` never passed the flag, so **since 2026-07-01 they asserted nothing**. (2) The keep is **Town-locked** but the scenario ordered it **day 0 at Hamlet** → rejected → `RaidSpawnSystem` short-circuits on `keepPosition === null` → no raid clock at all. **Not a silent reject:** the run *printed* `a keep needs Town tier`, then **exited 0** with a cheerful economy summary — the fixture logged its own failure and reported success. (3) `popCap 6`-vs-24 was **created by fixing (1)**: the sharp path un-gates sharp fire (which razes), and the old 3-tile pitch sat every house inside the ignition window — cozy fire had been masking a layout that couldn't survive the rules the fixture tests. (4) **Brief 110's 192×192 world doubled the raider march** (~15 days in transit, not ~7), so the "40 days is enough" comment was arithmetic on the old map. **Fixed as a real playthrough, not a forced flag** — tier deliberately **not** pre-unlocked (`TIER_LOCK` is the gate Challenge must honour); the town is re-laid on a **4-col/5-row lattice** that holds every wooden building to ≤2 wooden neighbours inside the ignition window, so fire is *structurally impossible* — **fireproof by layout**, the lesson sharp fire exists to teach. Grows honestly, earns Town day 12, raises the keep through the real gate, **sacked day 50**; `sack` now defaults to 70 days, prints PASS/FAIL and **exits 1 on failure**. **⚠️ The lesson: three guards, three different claims.** Two sharp-sack tests passed throughout the rot because they **poke `lp.tier = "Town"`** and bypass `TIER_LOCK` — the byte-identity guard proves the path is **unchanged**, those two prove **the math works**, and **nothing proved a player could get there**. New `sharp-raid-path.test.ts` covers reachability (never assigns `tier`; fails on any broken link in grow→Town→keep→raid→sacked); scope notes added to both older guards. Cozy byte-identical; 270 tests. **Unblocks 103**; de-risks 113 (whose "sharp resolution stays reachable" was otherwise *vacuously satisfiable*).

> **2026-07-11 Brief 98 DONE — the market wall finally closes a trade (Option A, wired).** Wave 2b; senior/opus. The wall had been **charging AP for a loop that never closed**: `BUY_REQUEST` was forwarded to the seller's inbox and consumed by nobody, `TRADE_COMPLETED` was never sent in production, the `marketOffers` belief three personalities gate on was written **only by test fixtures**, and `sell-from-wall` had an AP cost with no ActSystem case. **The load-bearing choice is escrow-at-post:** `POST_OFFER` debits the seller immediately (via a new `debitCropDetailed`, which reports *which* quality tiers it consumed) and parks those tiers on the offer, so **the wall — not the seller — owns listed stock**. Three bugs die at once: an uncovered listing is never stored (no oversell), two buyers racing one offer cannot both be filled (`settleBuy` is all-or-nothing, then deletes the offer), and "the seller's stock vanished before settlement" becomes *unrepresentable* because the goods already left their inventory. The new `WallTradeSystem` consumes `BUY_REQUEST` in the **SNOOP band — the only slot that works**, after `InboxDispatchSystem` delivers the message and before `PerceiveSystem` unconditionally wipes every farmer inbox (its `TRADE_COMPLETED` is `send`-queued, so trust/event-feed snoop it next tick; the band is load-bearing, position within it is not). Settlement uses the **offer's** price, never the buyer's claimed one. Escrow returns on `CANCEL_OFFER`, on the `OFFER_TTL_DAYS = 3` sweep, or via the new `sell-from-wall` intent, so `offersById` is bounded and no stock strands at run end. Crop movement routes through brief 99's `debitCrop`/`bankHarvest`, not a hand-rolled decrement. **The acceptance bar was a run, not a code read: 42 / 36 / 40 completed wall trades on seeds `0xc0ffee` / `1` / `42`** over 40 days — goods now circulate peer-to-peer at sellers' prices instead of only through the shopkeeper's haircut, and listed crops leave the net-worth leaderboard until they sell or the TTL returns them (baseline moved by design). Gates: typecheck 0; farm sim-core **834** (+5: conservation, bounded offers, escrow-rejects-uncovered, insolvent-buyer-moves-nothing, two-buyer race); full-repo test exit 0; determinism **MATCH ×3** (no new RNG draws). [economy.md](economy.md) + [system-ordering.md](system-ordering.md) updated. **Wave 2 complete.** Next: **Wave 3** (102 disease counterplay, 113 raid body) — and the [`sack` drift](../todos/closed/2026-07-10-citadel-sack-scenario-drift.md) still blocks 103.

> **2026-07-11 Brief 99 DONE — the P2 debt batch (review findings 28–34 + `maxDays`).** Wave 2a of the [dispatch plan](../todos/closed/2026-07-10-remaining-work-dispatch-plan.md); 5 chunks on disjoint file lanes (4 junior/Sonnet + **1 senior/opus on the rng+auction lane**, promoted because the determinism gate cannot distinguish a *correct* rng change from a *wrong-but-still-deterministic* one — the gate is blind exactly where that chunk works). **28:** `debitCrop` centralized in `economy/helpers.ts` (silver→normal→gold drain, `preferQuality` override); **`moveNormalQuality` deleted** — it only ever touched the `normal` tier regardless of what the giver held, which *was* the phantom-tier bug; zero `crops[…] -=` sites now survive outside the helper. **29:** harbor `deliveryDay` takes injected `ticksPerDay`. **30:** the `deliver-contract` paid no-op + its 3-AP row and the entire CNP contract-net (`cnp-coordinator`, `cnp-registry`, `protocols/cnp`) **deleted** (decided: delete, don't wire). **31:** ShopSlateSystem draws from a named `fork("shop-slate")`; the auction takes the **runner-up ladder, not escrow** (escrow would reach into farmer inventory on every bid *and* duplicate the shopkeeper's gold accounting, racing its own debit); the festival tie-break now **spends** the draw it was already taking (uniform among tied leaders) instead of discarding it, killing a low-id bias at zero draw-count cost; `EventFeedSystem.seen` + `settledAuctions` bounded. **32:** `defaultSpriteState` singleton dropped, `buildEvents` returns a fresh array. **33/34:** ProductionSystem's O(villagers × buildings) per-tick scan → one tile→building map/tick; FireSystem firebreak precomputed daily; `extendTrail` incremental; `device.lost` dedup — **byte-identical**, proven against the pre-wave commit in a throwaway worktree. **`maxDays` (#18) deleted** — `loadFromSave` computed its own only to pass it through; never in `CitadelSave`, so no save-format change. **The Farm baseline moved and explains itself** (40d, `0xc0ffee`): unsold crops **13,404 → 9,037** while gold **21,475 → 19,687** — more crops selling for *less* is precisely item 28's predicted mispricing being corrected; weather flips day 1, the fingerprint of the shop-slate fork no longer draining the shared stream. **One adjudication:** the wave's single red test was item 32's own, and the *test* was wrong — it probed `lastFacing`, which is only written when a farmer is *moving*, so an empty map in a fresh sim is correct; repointed at `lastIntention`. Gates: typecheck 0; farm sim-core 829/829, citadel sim-core 267, citadel client 471, engine core 184, farm client 196, farm server 31, citadel server 10; Farm determinism **MATCH ×3** (baseline moved by design), Citadel **MATCH ×3 + byte-identical**. Next: **brief 98** (market wall). See [log.md](../log.md).

> **2026-07-10 Wave 1 DONE (briefs 106, 104, 105) — the Citadel client render polish, dispatched.** First wave of the [remaining-work dispatch plan](../todos/closed/2026-07-10-remaining-work-dispatch-plan.md); two parallel `plan-split-dispatch` junior/Sonnet chunks on disjoint file lanes (one owning `main.ts`+`ui/`, one owning `render/*`), zero sim/determinism impact. **106** (`242dbbe`): the last gameplay DOM readouts (siege threat/defense/keep, hazard fire/disease, mode) migrate onto a retained `@engine/ui` widget (`ui/siege-hud.ts`, built-once + `refresh` per frame, EDG-only, aria-live mirror `#ui-a11y-siege`, click-consuming) — **zero gameplay DOM UI left in the Citadel client except the load/save file-input**. **104** all four items now done (`26deb45`): item 2 = a **hysteretic L/R facing flip** (`VillagerHeadingTracker` commits a new facing only when the smoothed `|ux|` clears `FACING_FLIP_DEADZONE=0.3`, holds when idle/near-vertical; applied via the pre-existing `Canvas2dSprite.flipX`), item 4 = ambient-crowd **cadence parity** (crowd bob routed through the same `gaitOffset` villagers use); items 1+3 were already live. **105 scope 1** done (`26deb45`): ambient pedestrians made **clearly non-villager** — `PED_SIZE` `0.8→0.6` tile + `alpha 0.55` — so the background crowd no longer inflates perceived population; **the dim is the sprite's own `alpha`, not the tint's alpha byte, because the WebGPU backend discards tint alpha** (traced, would have silently no-op'd). **105 scope 2 (MP owner-filter) stays PARKED** with MP (#21). Gates: typecheck 0; `@citadel/client` 471/471; `@engine/core` palette guard 184/184; browser-verified on real WebGPU (106 HUD live; clean render, figures on roads, no smearing, `reloads:0`) — the facing hysteresis + bob parity are additionally pinned by numeric tests. Next in the plan: **Wave 2** (Farm sim-core economy: 99 then 98). See [log.md](../log.md).

> **2026-07-10 Brief 100 DONE — the two-way service loop finally has its upside.** The OpenTTD downside shipped in 2026-06-27 and was softened into a throttle by Phase H; the reward for a *well-served* building never existed. It does now, as **one curve**: `bufferServiceFactor(buffer, cap, serviceEma)` **replaces** `bufferThrottleFactor` at the call site — above the 0.6 fill knee it *is* the throttle, below it a producer whose rolling service EWMA clears `SERVICE_BONUS_BAND = 0.75` ramps to `PRODUCTIVITY_BONUS_CEIL = 1.25` (thriving/starved = `2.08×`; nothing ever returns 0, downside rule #9). Service coverage also re-weights the immigration roll (`arrivalFactor`, `SERVICE_ARRIVAL_WEIGHT = 0.1` carved *out of* the existing `0.7..1.0` band, not added beside it), so happiness alone now tops out at `0.9` and a stocked-but-stagnant town visibly stops attracting people. **The headline finding is that a rounding change nearly took the credit for the mechanic.** The WIP floored output once at the end, carrying the remainder across *every* multiplier; measured on the 60-day `grow` run (3 houses → popCap 18, so pop is food- not housing-limited): `main` **9–10**, global-carry-with-bonus-neutralized **14**, global carry + bonus **15**, carry scoped to the service factor + bonus **12**. The carry alone did the work, the brief's mechanic was worth one villager, and it was silently re-tuning happiness / town-hall lift / seasonal grain — three numbers set in a floor-per-step world. **Scoped** to the service factor the bonus still pays out (9–10 → 12, in-band), disproving the WIP's own justification that a global carry was required; `outputRemainder` stays 0 whenever the curve returns exactly 1, so a building not earning the bonus is bit-for-bit unchanged. **Two bugs no test would have caught:** (1) the EWMA was sampled at the cycle timer, *above* the converter input-draw guard — a flourless bakery's buffer is empty *because it never baked*, so it climbed to `serviceEma ≈ 1.0`, would have collected the bonus the instant flour returned, and would have lit the new render cue while starving; it is now folded in at the **emit**. (2) The `starve` scenario stopped starving, so the fixture was **re-laid badly on purpose** (each producer at the end of a 16-tile spoke): connected, but hauler round trips dominate, buffers back up, the EWMA never clears the band, and it starves *because of how it was built* — the brief's thesis as a fixture. (`sack` already failed to sack **before** this brief — pre-existing drift.) Legibility: `BuildingSnapshot.wellServed` → a breathing cream ground pool (`wellServedGlowQuads`), day and night, strengthened from one ring at `alpha 0.1` (invisible over the road carpet on a real GPU) to a two-ring falloff at `0.18`. Gates: typecheck 0, **2108 tests**, Citadel determinism **MATCH ×3**, `grow` 60d **pop 12/18** (baseline moved by design), `starve` → `gameOver=true`, **browser-verified** (4 producers `wellServed`; 0 houses / 0 unstaffed / 0 roads). Next per #26: **{102, 99, 106, 104, 105, 98}**. See [log.md](../log.md).

> **2026-07-10 Brief 110 DONE — the solo world is 192×192 and the windowed bake is live (`0fd66c0`, after part 1 `8e930f3`).** Decision #22 executed; **browser-verified on a real WebGPU GPU** (11/12 checks; the 12th is a missing `favicon.ico`). `terrain()` → 192×192, `windowed` → **true**, panning all four corners re-bakes 6× with `IncrementalQueue.pending` draining to 0 each time and the baked window containing the camera every time; a house at tile **(100,100)** — past the old 96 bound — lands and renders registered with its terrain (brief 108 item 4 / findings item 35, verified in solo). **Three bugs surfaced that no test could see.** (1) **Resource density was keyed to the *mutable default* world size**: `areaScale = w·h / (WORLD_WIDTH·WORLD_HEIGHT)` meant the default world always scored 1 however large it grew, so raising the default silently **quartered resource density game-wide** — nearest-grove walk doubled (forest p50 17→41, stone 22→57) with every test green. Now anchored to a fixed `RESOURCE_DENSITY_REFERENCE_AREA = 96*96`. (2) **`tileKey` was non-injective at the east/west map edges at *every* world size** — `ty*W + W === (ty+1)*W + 0`, so an east-edge road reported a connection to column 0 of the next row; fixed with a constant `TILE_KEY_STRIDE = 4096` (the precedent `ambient-crowd.ts` already used). Two of three hardcoded `W = 96` strides in `sim-bootstrap.test.ts` had been **passing for the wrong reason**. (3) **`launchAttack` was ungated**, so flipping `enableArmy` would have *created* an unbounded-`state.armies` leak (proven: 20 commands → 20 armies, tools gone) rather than preventing one — flip + gate landed together per #23. Decision **#25**'s bound is **measured, not assumed**: over 100 seeds the 96×96 world's nearest-resource distance never exceeded 67, so `RESOURCE_MAX_DISTANCE = 70` makes the big world never worse than the small one ever was while repairing only ~5% of stone seeds (192 stone tail 86 → 70). Also: engine `assertTextureWithinLimits()` guards `maxTextureDimension2D` (an oversized bake used to paint **black** with nothing naming the world size); `init` now carries `worldWidth`/`worldHeight` so solo's client and worker cannot disagree. Gates: typecheck 0, **2081 tests**, Citadel determinism **MATCH ×3**, Farm MATCH untouched, headless Citadel baseline **unmoved** (`pop 9/12, bread 10`). Next: **brief 100** (economy growth). See [log.md](../log.md).

> **2026-07-10 (second grilling session) — MULTIPLAYER IS DEPRECATED; the solo world grows to 192×192.** Design; decisions **#21–#26** in [citadel-decisions.md](citadel-decisions.md). Hours after #11–#20 committed to making MP *correct*, the question underneath them — *who plays it* — had no answer: **#7** removed the score, **#9** the ending, **#15** the armies, **#17** the save, leaving a scoreless, endless, unsaveable co-op sandbox that four more briefs of infrastructure would have exposed on a VPS. **#21** deprecates MP (kept in-tree, compiling, unmaintained); its three known defects and the revival checklist now live on one page, [citadel-mp-deprecated.md](citadel-mp-deprecated.md). Consequences: **#22** nobody ever chose 256×256 on its merits (it was the server's default) — the **solo** world grows 96→**192×192**, the smallest size crossing the `4096²` iso-pixel windowing threshold, so brief 110 part 1 and briefs 21/22 stop being dead code; **#23** reverses #15 — with no MP there is nowhere for lethal PvP to relocate *to*, so `ArmySystem` freezes (`enableArmy` default → false) and its marching machinery is reborn as the **body of the existing cozy PvE raid** ([brief 113](../briefs/game/done/113-citadel-raid-gets-a-body.md), filed not built); **#24** Challenge mode is solo-only and no longer blocked on anything; **#25** `repairSolvability` must guarantee resources are *near* the core box, not merely *reachable* — on a 4× map a guaranteed stone can sit 100 tiles away and the Phase C cold open would open on a living town that cannot grow, invisible to every existing test; **#26** the world lands before the economy (brief 100's numbers are meaningless on a map about to quadruple). **Superseded, never built:** [109](../briefs/game/superseded/109-citadel-vps-deploy.md) (nothing to deploy), [111](../briefs/game/superseded/111-citadel-mp-room-keys-and-session-semantics.md) (no public room to key), [112](../briefs/game/superseded/112-citadel-cozy-mp-drop-armies.md) (moot — no cozy MP; its `enableArmy` default-flip + `launchAttack` gating survive into brief 110). **Reshaped:** [110](../briefs/game/done/110-citadel-client-world-size.md), [103](../briefs/game/done/103-citadel-challenge-mode.md) (solo-only), [105](../briefs/game/done/105-citadel-crowd-honesty-mp-owner-filter.md) (ambient-crowd half only). **Parked:** 101, 107 (Farm is in maintenance), engine 18, engine 19. **Order (#26): 110 → 100 → {102, 99, 106, 104, 105, 98}.**

> **2026-07-10 Brief 108 DONE — Citadel live-MP verification pass (`16b0191`).** First time MP was driven live: two real browser tabs on `?mp` against the WebSocket server, plus a raw-WS harness for timing the reap grace (browser tab churn is coarser than the 10s window and gives a false "fresh run"). **Item 1 room lifecycle PASSES** — join, late-join replay with correct `ownerId`, owner handoff in **211ms**, reconnect at 3.1s rejoins the live run / at 12s gets a fresh one (citadel-38 P1#7 verified). **Item 3 FAILED and is fixed:** the town-hall's keep/raid-anchor role was gated on `state.players.length > 1` — a *live* count — while `keepPosition` is assigned once, at placement. A real room is founded by **one** peer and grows, so the founder's hall never anchored: raid-spawn gates entirely on `keepPosition`, leaving them **permanently raid-immune**, while the snapshot's `keepPresent` re-evaluated the same predicate each tick and flipped true the moment a second peer joined — the founder was told "Keep: standing". Mode is now the bootstrap-time `CitadelSimOptions.multiplayer` flag (default false; server passes true, solo worker states false). Solo/headless/determinism unchanged **by construction** (both predicates are false at one player, no RNG draw between them); regression test proven to go red under the old rule. **Items 2/4/5 BLOCKED on a root cause the pass uncovered:** the server runs **256×256** but the **client is hardcoded to 96×96** (`generateTerrain(SEED)`; `iso.ts`'s `ISO_ORIGIN_X`/`ISO_WORLD_W/H` are compile-time consts) — so players are silently confined to the top-left corner, a hall at the world's own centre (128,128) lands off-canvas at y≈712, raiders spawn in untextured void, and `shouldWindow` is **always false**, making briefs 21/22's runtime unreachable (citadel-38 P1#8's "one-line fix" *was* applied at `main.ts:1221` and is **inert**). Review findings item 35 is real but **latent behind** it. Filed as [brief 110](../briefs/game/done/110-citadel-client-world-size.md), which carries 108's three blocked items as acceptance. Gates: typecheck 0, **2051/2051 tests**. Neither bug is visible from unit tests or solo play — that is the argument for this brief existing. See [log.md](../log.md).

> **2026-07-10 Brief 97 CLOSED — wave 2 landed (`c8ee284`), all ten chunks done.** Chunks 3, 5, 8 shipped via `plan-split-dispatch` (2 senior/opus on disjoint game lanes in parallel, then 1 junior/Sonnet serialized on the shared snapshot file; 3 scoped finders + 1 fix agent). **Farm station inbox leak**: `InboxDispatchSystem` fans every broadcast into every entity with an `inbox`, but `PerceiveSystem` clears only farmers (`query("inbox","beliefs","fsm")`) — a new `StationInboxClearSystem` drains the rest in a new final **`CLEANUP` band 10**, after the last consumer; `WeatherSystem` self-drains pre-dispatch; the shopkeeper retains a winner's un-credited `AUCTION_RESULT` (a live cross-tick settlement retry). Peak station inbox over 40 days: 11, flat in day count. **Citadel MP pause/speed authority**: snapshot now carries authoritative `paused`/`speed` + `isHost`, corrections re-broadcast immediately (no tick runs while paused, so a correction can't ride one), non-host controls disable, solo load-save self-corrects. **Citadel toast dedup + trade race**: monotonic `eventsSeq` replaces string-matching on a capped tail; `trade` sends offer *content*, not a positional index that raced the daily re-roll. **Gates:** typecheck 0, test 0 (all 10 workspaces), Farm + Citadel determinism MATCH ×3 each, and — unlike wave 1 — **both baselines byte-identical to `main`** on three seeds. Browser-verified (solo pause/speed/save/load). Review found **0 functional bugs** but caught that the toast regression test *passed on the unfixed code* and that the Farm inbox test guarded inbox *size* when the failure mode is message *visibility*; both guards were rewritten and proven to go red. A fix-pass regression (interp ingest gated on `tick > last` froze entities after a load-save, which rewinds `tick`) was caught and corrected to `!==`. See [log.md](../log.md) and [system-ordering.md](system-ordering.md).

> **2026-07-09 Repo gates repaired — `npm run typecheck` and `npm run test` both exit 0 (`42bb4b1`).** Five workspaces were typecheck-red on clean `main`, not the one that had been filed (`typecheck` runs `--workspaces` and stops at the first failure, so the reported one was never reached): the five headless packages that import the `@engine/core` barrel lacked `@webgpu/types` + a `*.wgsl?raw` declaration, which `@citadel/sim-core` and `@citadel/server` already carried. `farmer-frames.test.ts` had been dead at *import* since the reorg (stale `farm-valley/` atlas path) — re-pointed, both assertions pass. `check-determinism` now runs on Windows: the npm script uses the `--check-determinism` flag `env.ts` already supported, and the worker registers tsx **inside the thread** (tsx's ESM hooks do not propagate via `execArgv`). Two more red-by-default tests fixed: `interior-decor` (an `expect()` per pair in an O(n²) loop, 8.4s → 2.4s) and `coral-fishing.integration` (60s hook timeout for a ~40s-idle sim, load-flaky → 180s). **A red gate now means a real defect.** See [the closed todo](../todos/closed/2026-07-09-repo-gate-infrastructure-rot.md).

> **2026-07-09 Brief 97 wave 1 — review fix wave (P0/P1); 6 of 10 chunks landed.** Dispatched via `plan-split-dispatch` (opus controller; 1 senior/opus + 5 junior/Sonnet chunks on disjoint file lanes in one working tree; then 3 scoped review finders + 1 fix agent). **Landed:** ch.1 Farm server hardening (speed clamp 1..8, `tickRateHz` 1..60, detach-before-`attachInit`, `start()` try/catch + top-level rejection loggers, `maxPayload` 64 KiB, `isValidSwapIndex` closing a NaN hole); ch.2 Farm sim guards (village gate on `handleSellShopkeeper`; `GROWTH_DAYS[crop]` replaces a **self-cancelling** term that pinned `growthScore ≈ 1.0`, so season/skill multipliers never reached quality; carpenter wood refunded on both failed-delivery branches); ch.4 **Citadel ghost workers** (fire suppression sets an ephemeral `BuildingRuntimeState.suppressed` consumed by ProductionSystem instead of corrupting `workerCount`; new shared `releaseWorkersAt(state,x,y,w,h)` called from all four real removal sites — fire `_destroyBuilding`, siege `applyRaidDamage`, army `destroyBuilding`, player `demolish`); ch.6 Farm client render (juice diffs by **event-tick high-water mark** not `events.length` — the feed is a capped 30-entry tail, so juice went permanently dead after ~30 events; hitstop no longer halved by a double `getInterpolatedSprites()`; boat hull interpolates; connection-lost banner wired + **verified live in-browser**); ch.7 engine render (half-extent viewport cull, three per-frame `createBindGroup` calls hoisted to init, ghost pass batched); ch.9 Farm agents (kind-filtered gather gate, endgame liquidation sorts + adds bean/sleep, SEED_COST-ordered fallback ladder, port-hop gated, coral casts counted at execution). **Review found 5 real bugs invisible from inside any single lane** (see [log.md](../log.md)). **Gates:** engine 177/177, farm-server 31/31, farm-client 196/196, farm-sim-core 811/812, citadel-sim-core 231/231, citadel-client 423/423; Farm determinism MATCH ×3, Citadel MATCH ×3. **Farm baseline moved by design** (100d, seed 0xc0ffee, vs `main`: diverges day 2, all four AI personalities; total gold 81,962 → 55,412, unsold crops 36,134 → 28,008 — crops now actually sell, and quality finally responds to season/skill). **Wave 2 (ch.3/5/8) landed 2026-07-10 — brief 97 is now closed;** see the entry above.

> **2026-07-02 Citadel art-08..12 — fidelity fixes + layered-composite detail uplift (DONE).** The showcase-review wave shipped via `plan-split-dispatch` (opus controller; 4 Sonnet chunks + 1 opus chunk for art-12; 2 Sonnet finders, 0 bugs). **art-11** fixed a real roof-shading bug (`drawGableRoof`/`drawFlatCrenellatedTop` shaded on two fighting axes → front-left brightest under the UL sun; reordered so **back-left is brightest**, ridge kiss on the sunward crest) + a `roof.test.ts` up-left-gradient regression invariant. **art-08** refactored the windmill cone → **square plinth (`cubeBase`) + round drum + sails** (`drawRoundDrum` generalised, fort unchanged). **art-09** made the well a **centred square iso-box kerb** (the offset was really the intentional SE contact shadow). **art-10** moved market stalls to the **plot rim, open centre**. **art-12** (headline) added a first-class **`composite([...Layer])`** bake-time authoring path (painter-ordered sub-recipes → one atlas frame), rebuilt `boxBuilding` + `warehouse` from reusable `Layer` modules, and added detail modules (shuttered windows w/ muntins+sill, stone coursing + quoin cornerstones, chimney, ground apron) via **per-piece local density — NOT a global `ISO_ART_SCALE` bump** (atlas held at 256×4096, already the pow2 ceiling; **zero atlas growth**). A review cleanup removed `cubeBase`'s dead left-face slope term (a no-op that masked a latent well-metrics trap). Gates: **@citadel/client 420/420, palette 6/6, typecheck clean**, EDG32 + determinism preserved; **browser-verified in `?showcase`**. Commits `4091180` (art-08..11) + `ff05ebe` (art-12) on `main` (not pushed). See [log.md](../log.md). Follow-up polish noted (windmill drum reads a touch columnar vs the plinth; remaining forms — cottage/fort/church — not yet migrated to `composite`, a natural next slice).

> **2026-07-01 Citadel cozy pivot Phase I — terrain clustering + solvability guarantee (decision #10).** *Terrain IS the puzzle: a guaranteed-solvable floor with rich texture above it.* **The last structural pivot pass — A, B, C, D, G, H, I are ALL now done; the entire structural pivot has shipped** (only optional E/F remain). Built via `plan-split-dispatch` (2 senior/opus + 1 junior/Sonnet + 2 review finders + 1 opus fix). **(1) Clustering** ([terrain.ts](../../games/citadel/sim-core/src/world/terrain.ts)): the per-tile forest/stone fbm *sprinkle* → seeded **blob-centered patches** (groves + ore-veins) via a SEPARATE `resource-clusters` fork (river/lake stream byte-identical; `baseNoise` removed cleanly). ~0 singletons → woodcutter/quarry/mine placement is a real "build toward the resource" decision; resource-poor maps now occur (giving the Phase-G trading post a job). **(2) Solvability guarantee**: new pure `repairSolvability(cells,w,h)` at the end of `generateTerrain` guarantees a 12×6 all-buildable core box near center (carves if none) + ≥1 reachable Forest + ≥1 reachable Stone (4-connected flood-fill; paints a blob if missing/stranded). No RNG — pure fn of the grid. 100 seeds: 0 core-carve / 3 forest / 10 stone repair; **100/100 solvable + byte-identical**. **(3) Review fix (controller adjudication):** both finders flagged that `repairSolvability` and the Phase-C cold open (`seedFoundingTown`) ring-scanned the core box with different radius bounds (`/4` vs `max(W,H)`) → could anchor different boxes. Fixed by extracting ONE shared exported `findCoreBox`+`CORE_BOX_W/H` that BOTH call (full-grid scan → guarantee is a strict superset; carve targets the center box → **provably lockstep**); `seedFoundingTown`'s inline scan removed. Plus a degenerate-world edge fix (last-resort resource paint → box corner not center). Gates: **sim-core 220/220** (terrain.test.ts 10→19), typecheck-clean, **determinism MATCH ×3**. Not yet eyeballed in-browser (WebGPU headless). See [log.md](../log.md).

> **2026-07-01 Citadel cozy pivot Phase H — economy under the downside rule (decision #9).** *Nothing ever fully stops; every problem is a throttle toward a ~60–70% floor, always recoverable.* Grounding the plan in current code showed most of the brief's H text had **already shipped in Phases B & G** (winter grain floor in B; decree purge from production *and* needs-happiness in G), so H reduced to a focused **2-file inline change**. **(1) Throttle-not-halt** ([production.ts](../../games/citadel/sim-core/src/systems/production.ts)): the stockpile-pressure hard `continue` (full-buffer building went *dark*) became a pure `bufferThrottleFactor(buffer,cap)` — **full rate below a 60% fill knee, then a linear ramp to the 0.6 productivity floor, never 0** (unserved building *trickles* instead of shutting down). A genuinely-full buffer still hard-skips **before** the input draw (no converter wastes input), + a `Math.min(amount, cap-buffer)` clamp so the buffer never overflows. **(2) Single-slot** `farm/woodcutter/quarry/mine` (`workerSlots 2→1`; mill/bakery/sawmill/smith were already 1) — growth is purely spatial, freed worker staffs another building. **Controller adjudication:** the brief's "bump farm output 3→6 to compensate" was **CUT — premise wrong** (production is a per-building emit gated on `workerCount>0`; it never scaled with worker *count*, so dropping the dead slot leaves throughput unchanged; 6 would have *doubled* it). Kept `outputPerCycle:3`. Residual `activeDecrees` reads remain only in **frozen** immigration/siege (dead paths, later pass). Gates: **sim-core 212/212, typecheck-clean, determinism MATCH ×3** (0x1a2b3c4d/0xc0ffee/0x2a, 40d). **Baseline moved by design** (`pop 5/12,bread 8` → `pop 9/12,bread 10`, `gameOver=false`): town now **survives winter** (grain trickles, never 0) + **self-recovers** from starvation dips — the downside rule is a property of the math. Done inline (below dispatch threshold). **Phases A, B, C, D, G, H done; E, F, I open.** Not yet eyeballed in-browser (WebGPU headless). See [log.md](../log.md).

> **2026-07-01 Citadel cozy pivot Phase G — autonomy pass (civic buildings + player-driven trading post).** Implements decision #8 (*player sets placement + economic intent; the town autonomously handles all behavior*). **(1) Player-driven trading post:** the autonomous seeded caravan (`TraderSystem` / `rng.fork("trader")` / `TRADER_INTERVAL_DAYS`) is reframed — `traderPresent` now means "owns a **staffed + connected** tradingpost" and `traderOffers` is a deterministic ≤3-offer menu (no RNG); the `barter` command → `trade` (`{offerIndex}`); tithe `RELIEF_BARTER_THRESHOLD` sweetener retired; the old DOM trader panel replaced by a tiny in-canvas **InspectPanel trade box**. **(2) Net-new `public-square`** (2×2, SERVICE_RADII 8, `BUILD_COST wood:8`, `plaza()` iso sprite, `Square` build-bar button, `festival`→`EDG.green` coverage ring) autonomously lifts festival happiness (+15, spatial) for homes in reach — replacing the `festival` **decree**. **(3) Decree lever purged** from the cozy sim core: `setDecree` handler + festival/conscription constants + `_maintainDecrees` + decree-penalty block + `festivalDaysLeft` all deleted; work-hours +30% → **automatic town-hall-coverage output lift (×1.2)**; conscription halt gone. `activeDecrees` **kept but always-empty** (frozen immigration/siege still read it); client decree UI removed. **(4) Territory + army frozen** from the cozy/solo path (byte-identical): `TerritorySystem` gated on `enforceTerritory`; new `enableArmy?` option (default true, save/load round-trips) gates `ArmySystem`, solo passes `false`; MP unchanged, `army.test.ts` green unmodified. Built via `plan-split-dispatch` (2 senior + 4 junior; one BLOCKED→controller-resolved design call; a baseline-shift disease test re-pointed to its true invariant, not weakened; 3 review finders → 0 bugs, 2 cleanup nits fixed). Gates: **sim-core 211/211, client 387/387, typecheck-clean, determinism MATCH ×3** (baseline moved by design; new: `pop 5/12, bread 8`). **Phases A, B, C, D, G done; E, F, H, I open.** Not yet eyeballed in-browser (WebGPU headless). See [log.md](../log.md).

> **2026-07-01 Citadel cozy pivot Phase C — forgiving diegetic cold open (seeded alive core + threats deferred until grown).** The solo game now **opens on a living town** and the founding deadlock is **structurally impossible**. Two opt-in `bootstrapSim` flags, both **default OFF** (headless determinism baseline **byte-identical** — verified): **`seedTown`** pre-places a connected alive core (storehouse + farm→mill→bakery + house on a 12-tile road spine = **5 non-road buildings**) at (near) map center via the `placeOne(charge=false)` funnel **before the first tick** — a gift (occupancy/roadGrid/popCap apply, only the cost debit is skipped), **unlogged** (`loadFromSave` re-seeds by threading the flag, so no double-apply on replay); the existing day-0-anchored founding logic then spawns + staffs the pioneer within ~1–2 days. **`deferThreatsUntilBuildings`** (solo passes **6**) suppresses fire ignition / disease onset / raid scheduling until the player owns ≥N non-road buildings (new shared `countNonRoadBuildings` in tiers.ts; gate **short-circuits before any RNG draw** so flag=0 is byte-identical; only *fresh* onset is gated, active fires/outbreaks still progress+recover). Solo worker sets both flags + a founding wood grant; a **one-shot solo-only** camera reframe opens on the **actual seed centroid** (not geometric center — the anchor ring-shifts per seed) at MAX_ZOOM. Also fixed a stale Phase-D-winter-floor leftover (client `building-info` said winter yields "0 grain/day / nothing" → now floored ×0.5, never zero). Built via `plan-split-dispatch` (2 senior + 3 junior Sonnet 5 chunks + 1 Sonnet fix; 2 Sonnet review finders — finder A found no bugs, finder B caught 3 real integration issues, all fixed). Gates: **sim-core 218/218** (+8 seed-town +5 defer-threats), **client 381/381**, typecheck-clean, headless baseline unmoved. **Phases A, B, C, D done; E–I open.** ✅ **Phase C verified live in a real browser** (2026-07-01 `playtest-citadel`): opens on the seeded living town (camera framed on the actual seed centroid), 5 connected non-road buildings, villagers spawn+staff with no input, pop holds through day 63 (no deadlock, no death-out), `Threat/Fire/Disease` all clear (defer gate holds). Pop oscillates at housing-cap 6/6 — the *known pre-Phase-H economy*, not a cold-open bug. The thriving warm-glow contrast still unframed (the seed ships only the bread chain → house mood sits at neutral base 40; place services to see 40→80). See [the playtest log todo](../todos/closed/2026-07-01-citadel-phaseA-playtest-verification.md) + [log.md](../log.md).

> **2026-07-01 Citadel cozy pivot Phase D — threats demoted to recoverable happiness dips (freeze the bite).** The four threats no longer destroy/kill/sack; they **dent local happiness** (Phase-B productivity floor → visible slowdown that self-recovers), gated behind a new **`cozyThreats` bootstrap option (default ON)** so today's destructive path is **frozen, not deleted** (`false` = byte-identical sharp mode, for a future Challenge/MP mode). **Fire** smoulders → extinguishes (never razes; well speeds it; dents nearby house mood); **disease** slows → always recovers (never kills; guaranteed recovery floor); **raids** pilfer stockpile goods → leave (defense shrinks the theft; never sack/gameOver); **winter** grain floored 0.0→0.5 (unconditional; food always trickles). Gates: **sim-core 205/205, typecheck-clean, determinism MATCH ×3** (baseline moved by design). New `cozy-threats.test.ts` + a `cozyThreats:false` regression guard; the falsified winter-starvation economy test was **rewritten** to the new truth. **Phases A, B, D done; C, E–I open** — the Phase A cozy-look eyeball is now unblocked (fire no longer razes, disease no longer starves; C for the cold-open remains). Built via `plan-split-dispatch` with executor chunks on **Sonnet 5** (5/5 to-spec first-try; caught a cross-test bug; one first-run thoroughness wobble). See [log.md](../log.md).

> **2026-07-01 Citadel cozy pivot Phase B — happiness → productivity floor (the signal gets teeth).** Happiness + per-house mood became **stateful** (asymmetric ease toward target, recovery 0.45 / decay 0.30, ~2–3 day recovery → the floor is a property of the update rule); production output now scales `lerp(0.6, 1.0, h/100)` off the **local** worker's home-house mood, with a `Math.max(1, …)` floor so no producer ever drops to 0 (throttle, never cliff — fixed a base-1 smith/mine floor-to-0 found in review). **Determinism MATCH ×3** (seeds 0xc0ffee/0x1/0x2a); baseline moved by design (new: pop 5/12 Village, happy ~50). Threat re-pointing (D) + decree purge / winter grain floor (H) explicitly deferred. The town no longer death-spirals but isn't cozy-calm yet (volatile until D/H). Gates: sim-core 198/198, typecheck-clean. **Phases A–B done; C–I open.** See [log.md](../log.md).

> **2026-06-30 Citadel cozy pivot Phase A — per-house diegetic mood signal (FIRST gameplay phase of the pivot).** The [cozy-pivot build order](../todos/closed/2026-06-28-citadel-cozy-pivot-BUILD-ORDER.md) keystone shipped: the sim stops discarding its already-computed per-house `hasFaith/hasSafety/hasGoods` and derives a per-house `mood`, surfaced read-only on `BuildingSnapshot` (`{lacksFaith, lacksSafety, lacksGoods, mood}`); the renderer expresses it **diegetically** — warm `EDG.gold` glow + mood sprite-dim + mood-gated hearth smoke, so under-served houses read as neglected *without any overlay*. Determinism preserved (aggregate sim outputs byte-identical; render is off-sim). Gates: sim-core 184/184, client 381/381 (EDG32 guard + new wiring tests), typecheck-clean. **Phase A of A–I done; Phases B–I still open** (B = happiness→productivity floor reads this signal). **Not yet eyeballed in-browser** (WebGPU headless limitation). See [log.md](../log.md).

> **2026-06-30 `@engine/ui` framework + Citadel in-canvas UI shipping** ([brief 17](../briefs/engine/done/17-engine-ui-framework.md), branch `engine-ui-framework`). Dual-backend (WebGPU + Canvas2D) in-canvas UI package; Citadel top HUD bar (settlement readout + **all-goods strip** + speed/pause) renders in-canvas, **verified in-browser (WebGPU)**. **ALL 6 UI consumer todos landed** (resource-HUD pilot + all-goods strip, inspect-view + upgrade panel `2cab8ae`, villager-job `84a9ef9`, town-hall button + solo keep-anchor decouple, build-cost-hover + the `BUILD_COST` cozy economy with a founding wood grant) — all verified in-browser (WebGPU). The umbrella "all GUI in-game" todo continues: **toasts** (2026-06-30, + a reusable `renderTree` `opacity` channel) and the **build bar** (2026-06-30, in-canvas grouped text buttons + dispatcher + a11y mirror; new [authored-typography/icons todo](../todos/closed/2026-06-30-engine-ui-authored-typography-and-icons.md) to restore an icon grid) are migrated; the settings modal, minimap (Canvas2D), and occupancy badges are still DOM and need migrating onto `@engine/ui`.

> **2026-06-28 Citadel cozy pivot (design of record).** A grilling session resolved
> Citadel as **a cozy placement puzzle you read by watching the town live** — diegetic
> feedback, a cozy contract (nothing you built is taken from you), threats unified as
> happiness dips with a productivity floor (no spiral), and the 2026-06-26 sharp-pressure
> systems frozen (off-spec for the cozy core). Build order:
> [todos/2026-06-28-citadel-cozy-pivot-BUILD-ORDER.md](../todos/closed/2026-06-28-citadel-cozy-pivot-BUILD-ORDER.md).
> Supersedes the "fire punishes tight clusters by design" stance. Design only — no code changed.

> **2026-07-01 Citadel cozy pivot — ALL PHASES SHIPPED (A–I).** The full build order is
> delivered: A (per-house diegetic mood), B (happiness→productivity floor), C (forgiving
> cold open), D (threat demotion), G (autonomy / civic buildings + player-driven trading
> post), H (economy under the downside rule), I (terrain clustering + solvability), and now
> **E (per-villager mood)** + **F (motivation: inviting-gap pulse + contentment banner)** —
> the last two, both diegetic render-layer passes reading the keystone snapshot. Playtested
> in a real WebGPU browser 2026-07-01: a town now stays **alive, fed, and fire-recoverable
> across 200+ days** (never reachable pre-pivot); per-villager mood tracks home-house mood
> live. Two follow-ups filed (not blockers): **P1** cozy-path threat toast COPY still reads
> pressure-game ("caught fire!"/"starved") despite cozy-correct mechanics; **P2** the
> playtest driver can't yet read the in-canvas HUD or drive road-connected services (so
> Phase F's banner-edge live-fire is verified-by-mechanism, not yet scripted-live). See
> [todos/closed/2026-07-01-citadel-phaseEF-playtest.md](../todos/closed/2026-07-01-citadel-phaseEF-playtest.md).

> **2026-06-26 Citadel pass** (see [log.md](../log.md) for detail). Closed a batch of `todos/`:
> engine GC-churn hygiene (prealloc WebGPU draw scratch + double-buffer CommandQueue);
> the **citadel-38 audit** P0 MP-authority + P1#5 + P2 balance + P3 cleanup. *(Live-MP
> verification settled 2026-07-10 by [brief 108](../briefs/game/done/108-citadel-live-mp-verification.md):
> **#7 RunRegistry verified**; **#6 social/threat verified** — and a raid-anchor bug found +
> fixed; **#9 MP-render blocked** on [brief 110](../briefs/game/done/110-citadel-client-world-size.md).
> **P1#8 windowed-bake is wired but inert** — `shouldWindow` is always false because the client
> is hardcoded to 96×96.)*
> the **gameplay-depth trio** (siege probability bands + morale, scout/garrison-interceptor
> counterplay, threat→cadence/decree-gating/defense-pressure, hazard interlocks, festival +
> stacking decree counterplay, dynamic trader) — baseline moved by design, reproducible;
> and the **Citadel art todos** (entity-silhouette-legibility **built**; real-sprite-assets +
> procedural-building-detail **superseded** — the 2026-06-21 true-iso sprite library already
> delivers them). [true-isometric](../todos/closed/2026-06-21-citadel-true-isometric.md) is
> **done** (its GPU flat-box anomaly was confirmed a host-driver artifact, not a code bug).

**Where things stand:** engine briefs **01–16** and game briefs **01–89** are **Done or Superseded** — both `todo/` dirs are empty (2026-06-13). Briefs 85 (animation engine) + 89 (detailed 24×24 characters + held hotbar tool) shipped their phases and were closed **superseded** with only an optional in-browser feel-check + a 24px action pass left (WebGPU-only render → can't feel-check headless). The 2026-06-12 build-order todos (grow→décor→combat→ports→BDI-jitter→underwater→walls/lights→seasonal trees) are all complete; the two unrun calibration todos (rival-cutoff, combat-frequency) were closed **won't-do** in `todos/closed/`.

**Seasonal trees + big-tree island (2026-06-12)** — `seasonalTreeFrame` is now 4-way (blossom/green/autumn/bare) over tree/bush/fruit-tree/big-tree (instant swap); new atlas recipes for each; mature orchards now actually show the mature frame (was a latent sapling bug). New `big-tree` landmark island (bridged to the volcano) with a bespoke 48×64 seasonal big tree as a baked centerpiece. **World/render todo group COMPLETE** (grow, theme, bigger neutrals, ranches, casino, seasonal/big-tree). Animated season cross-fade deferred to a brief-85 consumer.

**Casino open-air (2026-06-12)** — casino building removed; dressed with a deliberate layout of 5 new EDG32 gaming sprites (slot-machine ×2, roulette, blackjack, dice, shell-game) as bigger island-locked baked props (not random scatter); leftover neon-glint particle emitter removed. Fixed a grow regression while here: baked `BIG_STRUCTURES` (forge/carpenter/weather/volcano) had stale 160-coords and rendered in ocean post-grow — now island-locked via `scaleAroundNearestIsland`, guarded by a geometry.test.

**Per-farm ranch islands (2026-06-12)** — each of the 21 farms has a neighbouring `ranch-N` island (theme `'ranch'`) hosting its livestock pens (relocated from the home farm via `handleBuildPen` → `ranchForFarm`). Placed cardinal-adjacent (outward infeasible — the grow left no outer margin), deterministic search for in-bounds + ≥2 gap + clean bridge. **Tend is now gated on being at the ranch** (`handleTend` + a travel-to-ranch step in `deliberateTendPens`) so ranches get real daily AI traffic. 1071 repo tests green.

**Bigger decorated neutral islands (2026-06-12)** — heritage/mushroom/ice/volcano/casino grown 8×8→12×12, camp/weather grown less (tight pair); themed for interior décor. shrine/waterfall + functional fishing/harbor/mill left as-is.

**Themed interior décor (2026-06-12, foundation #0.5)** — `RegionDef.theme` typed enum (render-only, never read by sim) + [interior-decor.ts](../../games/farm/sim-core/src/render-systems/interior-decor.ts) `computeInteriorDecor(world)`: per-theme blue-noise scatter inside themed regions, baked into the static layer (layer 2). Forbidden-set (from world queries) dodges every functional tile + bridge mouths; deterministic per `WORLD_GEN_SEED`. The substrate the décor todos (bigger neutral islands / ranch / casino / big-tree) build on. 703 sim-core tests green.

**World grown 160→240 (2026-06-12)** — foundation for the land-adding todos (see [todos/closed/2026-06-12-00-foundation-grow-grid-to-240.md](../todos/closed/2026-06-12-00-foundation-grow-grid-to-240.md), now done). Uniform position-only scale (SCALE=1.5, center 80→120): island `bounds` run through `scaleB` (center moves ×SCALE, **size preserved** → gaps open ×1.5); on-island content (décor/stations/footprints/dock+tavern/festival tiles + render-overlay anchors) is locked to its island via `scaleAroundNearestIsland` so nothing drifts into the ocean; coral reefs derive from live fishing-isle bounds. One hand-tune: shrine +2x to keep the village↔shrine bridge. `DEFAULT_ZOOM` 2→3. The todo's "only one stray literal" estimate was wrong — the real blast radius was dozens of hardcoded 160-coords across setup.ts/coral.ts/shared.ts/regions.ts anchors, all now island-relative or bounds-derived. Full guard-test pass + repo tests (1058) + typecheck green; render eyeballed OK by the user; determinism check waived by the user.

**Improvement backlog shipped 2026-06-12** (filed and implemented the same day — one worktree branch per brief, Sonnet executors, merged to main individually, tests + typecheck green after every merge):
- **Engine 10** WASM allocator fault — root cause: the TS wrapper freed `gridPtr` before `outPtr`, but the AS **stub bump allocator only reclaims the most-recent allocation**, so ~25.6 KB leaked per `findPath` (160×160 grid) → heap exhausted at ~655 calls → `unreachable`. Fix is a two-line free-order swap in [pathfinder.ts](../../engine/core/src/wasm/pathfinder.ts) + an 800-call churn regression test (red pre-fix). TravelSystem's per-intent catch kept but loudened to `console.error`. **Fast diff MATCH ×6** (seeds 0xc0ffee/1/42 × ticks 20/1200), and 3-day output is **byte-identical to pre-fix main** (the leak never bit in short runs) — no baseline move.
- **Engine 11** WGSL validation guard — `wgsl_reflect` 1.4.0 (pinned devDep) parses every `*.wgsl` in [wgsl-lint.test.ts](../../engine/core/src/render/webgpu/shaders/wgsl-lint.test.ts) (globbed, with throw-fixtures proving it bites); the reserved-keyword regex scan is **kept** — the parser does not catch that class (the original black-screen incident).
- **Engine 12–16** shader wave (all render-only, EDG-safe by construction): **12** GPU day/night tint pass (`tint.wgsl`/`tint-pass.ts`; wash off the 2D overlay, which keeps only the Canvas2D fallback jobs; later passes compose under it) · **13** living water (cell-hash tiling break + value-noise UV-warp + step()-quantized shore foam + Voronoi caustics, masked by an R8 depth-mask texture built game-side from water-depth data) · **14** weather/particle parity (round SDF snow, rain-streak tail taper, per-flake hash variation, true 8-point star, shaped fade; CPU particle pool capped at 512 + swap-remove; ring splashes skipped — no GPU splash instances exist) · **15** fBm cloud-shadow pass, world-anchored, weather-driven coverage (sunny 0.06 → storm 0.72) · **16** per-instance vertex wind sway for crops/trees/bushes (top-vertex shear, deterministic per-tile phase, global gust knob; bridge rope kept its richer two-sine CPU sway).
- **Game 86** juice — pooled DOM gold popups (per-kind caps), trauma² screen shake (≤3 px, post-smoothing offset, positive beats only), 2–4-frame hitstop via `SimClient.freezeInterp` (snapshot consumption untouched), leaderboard score-bump (easeOutBack), drama-weighted intensity, and a resync/H-skip cursor guard so stale events never replay as a popup burst. 30 new jsdom tests.
- **Game 87** home/forge-house Stardew restyle — `home` corrected 16×16→32×48 (tests already treated it as 32×48), `forge-house` re-trimmed with chimney kept at the same pixels (`FORGE_CHIMNEY_PX` anchor verified); only the buildings sheet rebuilt (rest cached, brief-71 invariant held).
- **Game 88** real-VPS deploy — **closed without code: the user executed the deploy on real hardware and confirmed it works.**

⚠️ **Pending:** an in-browser visual pass over 12–16/86/87 (WebGPU can't render headless on this box) — see each done-brief's acceptance list for what to eyeball. [83-visual-depth-polish](../briefs/game/done/83-visual-depth-polish.md) is **Done** (user-confirmed visuals, 2026-06-12): granular near-shore water, sandy-shore descent, sagging bridge guard-rope with organic sway, procedural WebGPU water, and the Stardew restyle of all 5 cottages (the `home`/`forge-house` leftover closed by brief 87 above). The game is **WebGPU-only** (`farm-valley` forces the backend; Canvas2dRenderer kept in `@engine/core` for tests); the `webgpu-migration` branch was fast-forward-merged into `main` (2026-06-12). The old "WGSL isn't validated by tsc/vitest" gotcha is now mitigated by the brief-11 parser guard. Tests + typecheck green (one atlas cache test times out on constrained hardware while the dev server runs — environmental). **No open threads.**

*Resolved 2026-06-12 — **Tier-0 FPS regression** (brief 84): NOT reproducible on real hardware. A user real-GPU `?profile` export (`ANGLE / AMD Radeon`) shows **99 fps, `frame` JS 5 ms** — the old "15–30 fps" was a headless-SwiftShader (CPU-raster) artifact. Shipped: `DEFAULT_ZOOM = 2` (framed-in opening shot, kept by user choice; perf value now insurance for weak GPUs/high-DPI) + a `?profile` profile-export button (`window.__exportProfile()` + WebGL GPU-identity probe). No GPU-overdraw work needed. See [performance.md](performance.md) Tier 0.*

*Resolved 2026-06-12 — **Movers teleport** (brief 82): the `interpolate` flag was farmer-only, so work NPCs snapped tile-to-tile. Fixed by widening the flag to work NPCs + a 2-tile teleport clamp in the client lerp path. Render-only, no baseline move. (Livestock/boats/ambient were ruled out by code-reading — they don't tile-step.)*

*Resolved 2026-06-12 — **AI fishing broken** (brief 80): `FISHING_CAST_TILES` was already converted from the stale off-isle literal to a `deriveFishingCastTiles()` scan of the live isle bounds in commit `672cccd` (brief just wasn't closed). Verified + closed: live-world guard test passes and a new `fishing.test.ts` confirms `deliberateFishing` fires to a valid cast tile. Baseline already moved at `672cccd`, not this session.*

## Recent briefs (one-liners; detail in log.md)

**66–79 — shipped 2026-06-11 (Opus-plan / Sonnet-execute, committed per-brief):**
- **66** tab-resync — `SimClient` drops the straddling snapshot pair on tab-hide, resets the interp clock on show (render/transport).
- **67** pixel-snap + camera smoothing — engine `expSmooth` + `Canvas2dRenderer.pixelSnap`; lock-follow with glide-on-jump (render-only, bake byte-identical).
- **68** ambient idle life — seeded `AmbientLayer` (birds/leaves/chimney smoke), capped pools, no `Math.random` (render-only).
- **69** named system stages — `scheduler.stage()` labels + opt-in same-stage read/write bus audit; flattened order byte-identical (order-pin test).
- **70** startgold +30 uniform — cash constraint lifted (zero `would-breach-reserve`); ⚠️ baseline moved (reproducibility ×3); 15-day-close target unmet (gate is stock/encounters, not gold).
- **71** per-asset atlas recipes — recipe monolith → one file per asset + hash-cached per-sheet builds (`0 built, 6 cached` on a clean tree); see [asset-pipeline.md](asset-pipeline.md).
- **72** shared-run lobby server — `RunRegistry`: one `SimHost` per run-key, encode-once fan-out, owner-only control, late-join replay, zero-socket reaping. Determinism untouched.
- **73** travel-reachability guards — build-time connectivity-component map + gather-beat reachability guards; root cause was `TAVERN_GATHER_TILE`/`FESTIVAL_PODIUM_TILE` pointing at OCEAN in the radial world. ⚠️ baseline moved. Task 4 (WASM allocator fault) deferred.
- **74** weather-station island — new bridged `landmark`-kind region + building/antenna/beacon (render; ⚠️ may move baseline).
- **75** economy rebalance — principled [economy.md](economy.md) model (1 AP = one basic-labour action) + crop re-tune (spread 2.64×→1.59×). ⚠️ baseline moved by design; arc healthy.
- **76** loading screen — overlay covers Start→first-full-frame, dismissed off real readiness (main-thread only).
- **77** building 3D + farmhouses — weather-station hipped roof (48×48) + 5 personality-keyed cottages baked per farm (render-only).
- **78** Pip-movement — reported breakage **not reproducible**; root cause was duplicate dev processes (a spectator socket swallowed input). Regression guard added in `run-registry.test.ts`.
- **79** click-to-target + action cursor — Pip acts on a clicked tile (Chebyshev ≤ 1), slot-generic CSS cursors, pan moved to middle/right-drag.

**55–65 — 2026-06-10:** client/server split (55–58, see below); peer-interaction fix (59, see *sim behaviour*); then a render-polish wave — 60 max-zoom 3→6, 61 continuous Pip movement + AABB, 62 per-island floors, 63 zoom-out water-shimmer fix, 64 water swell + foam breathing, 65 cliff skirts. Only 59 + 61 touch sim behaviour.

> Engine brief 09 (perf) closed 2026-06-10 after a post-split re-profile answered its deferred gates — measured analysis in [performance.md](performance.md). Pip + interaction work shipped without briefs — see [player-and-interaction.md](player-and-interaction.md).

## Engine briefs — all Done/Superseded

| Brief | Status |
|---|---|
| [01-tilemap](../briefs/engine/superseded/01-tilemap.md) | **Superseded** — WebGPU dropped; Canvas2D took over. |
| [02-input](../briefs/engine/done/02-input.md) · [03-tests](../briefs/engine/done/03-tests.md) · [04-spatial-anim](../briefs/engine/done/04-spatial-anim.md) | **Done (partial — animation/spatial halves undone)** — input + engine test suites shipped. Brief 04's `SpatialHashGrid` + `AnimationClip`/`Animator` were built (`0919cbc`) then **deleted as unused** in the `cleanup` commit `1d5f80c` (2026-06-04); `engine/core/src/{spatial,animation}/` no longer exist. The animation primitive is being reintroduced **with consumers** by game brief 85 — see [wiki/animation.md](animation.md). |
| [05-pathfinder-into-movement](../briefs/engine/done/05-pathfinder-into-movement.md) | **Done** — WASM pathfinder confirmed load-bearing in `TravelSystem`. |
| [06-determinism-harness-and-analytics](../briefs/engine/done/06-determinism-harness-and-analytics.md) | **Done** — `CHECK_DETERMINISM=1` (MATCH/DIVERGE) + `EXPORT=csv\|json` in run-sim. |
| [07-chunked-tile-layer](../briefs/engine/done/07-chunked-tile-layer.md) | **Done** — `bakeStaticLayer` bakes the backdrop once; chunking unneeded. |
| [08-wasm-expansion](../briefs/engine/done/08-wasm-expansion.md) | **Done** — noise/rng/floodfill WASM; fixed the never-transferred-pathfinder bug. |
| [09-perf-optimization](../briefs/engine/done/09-perf-optimization.md) | **Done** — closed 2026-06-10; measured analysis in [performance.md](performance.md). |
| [10-wasm-pathfinder-allocator-fault](../briefs/engine/done/10-wasm-pathfinder-allocator-fault.md) · [11-wgsl-validation-guard](../briefs/engine/done/11-wgsl-validation-guard.md) · [12–16 shader-wave](../briefs/engine/done/12-16-shader-wave.md) (day/night wash · living water · weather parity · cloud shadows · foliage sway) | **Done** — 2026-06-12 improvement wave (one worktree branch per brief; 12–16 merged into one rollup); details in "Where things stand" above + [log.md](../log.md). |

## Game briefs 01–48 — all Done

Foundational (01–23): personalities, weather/crops, market/shop, observer UI, regions + travel, spatial market, render, slate sales, peer trades, trust + endgame, focus camera, leaderboard, walk/meet/slate UI, playback controls, save/replay, seed picker, decision trace, event feed, complete auctions, seasons, mid-game shock. All **Done** — files in [../briefs/game/done/](../briefs/game/done/).

| Brief | One-line |
|---|---|
| [24-auction-bidding-golden-bean](../briefs/game/done/24-auction-bidding-golden-bean.md) | Agents bid; `golden_bean` valuable (resell/gift). Fixed the "no winner" dead auction. |
| [25 panel-overlap-fix](../briefs/game/done/11-25-spectator-ui.md) | Observer + feed share one right-column flex container. *(merged into the 11–20+25 spectator-UI rollup.)* |
| [26-day-night-seasonal-grading](../briefs/game/done/26-day-night-seasonal-grading.md) | Render-side day/night + seasonal wash (tick-synced, sim untouched). |
| [27-long-days-intraday-timeline](../briefs/game/done/27-long-days-intraday-timeline.md) | `ticksPerDay` 20→1200; intra-day phases + SLEEP; economy stays day-denominated. |
| [28-ap-economy-rework](../briefs/game/done/28-ap-economy-rework.md) | AP `100+2·day`, sleep-gated, free travel, friend discounts. |
| [29-irrigation-crop-death](../briefs/game/done/29-irrigation-crop-death.md) | Watering required (2-day grace); rain auto-waters; survival-reflex keeps deaths ~0. |
| [30-procedural-ground-texture](../briefs/game/done/30-procedural-ground-texture.md) | Per-tile value-noise baked into the static layer. |
| [32-rendering-overhaul](../briefs/game/done/32-rendering-overhaul.md) | Y-sort, shadows, particles, walk/work/idle-bob, bigger atlas. Orthographic. |
| [33-world-expansion](../briefs/game/done/33-world-expansion.md) | 11 regions, tool system, watering can, resource drops, decorations, plot decay. |
| [35-player-activity](../briefs/game/done/35-player-activity.md) | Slower movement, action time cost, home/sleep routine, market visits, debug player. |
| [36–40 spectator-story-layer](../briefs/game/done/36-40-spectator-story-layer.md) | 36 day-100 "Legends" recap (`RunHistorySystem`/`summarizeRun`); 37 relationship matrix + `RivalrySystem` *(was DORMANT until brief 59)*; 38 per-event `drama` weighting → feed ★/rank-flip/recap headline; 39 multi-line `wealthSeries` chart + crossings; 40 intention bubbles + `skipToHighlight` (H) + feed-click-to-zoom. |
| [41–46 gameplay-depth-wave](../briefs/game/done/41-46-gameplay-depth-wave.md) | 41 8 season-gated crops + Normal/Silver/Gold quality (forked rng) — **the spine**; 42 coops/barns + perennial orchards; 43 season-immune greenhouse + 4 per-farm skills; 44 working carpenter/blacksmith commissions + tavern + notice-board; 45 season-variant tiles + festivals (days 13/38/63/88, deterministic harvest contest); 46 harbor + dockmaster + time-boxed contract economy. |
| [48-boats-and-coral-fishing](../briefs/game/done/48-boats-and-coral-fishing.md) | Per-farm boats + a separate boat-travel grid; two coral reefs with boat-only fish (`coral-trout`/`lobster`). `deliberateCoralFishing` in all 4 personalities. |

*(Briefs 49–79 are one-lined in the "Recent briefs" section above.)*

## Current sim behaviour & determinism

- **Tests/typecheck:** green; latest counts in the newest [log.md](../log.md) entry (not tracked here). farm-valley runs node-by-default with jsdom scoped to the ~9 DOM test files (vitest `projects`); `CHECK_DETERMINISM` runs its passes in parallel `worker_threads`.
- **Determinism is load-bearing**, verified `MATCH ×3` (seeds `0xc0ffee/1/42`). The contract is *same seed reproduces itself byte-for-byte* — **not** equality to pre-change numbers. The 2026-06-09 radial reorg, briefs 41–46/48 (new systems), and 70/73/74/75 (balance/region/economy) each **re-baselined outcomes by design**; reproducibility was re-verified each time. Brief 48 verified MATCH ×3 at both `ticksPerDay=20` and `1200` (raw `Math.random` in ACT paths is a nondeterminism bomb — fishing/mining use forked rng channels; grep confirms zero `Math.random` in sim-core source).
- **Headless-probe pitfall:** a headless `bootstrapSim` check **must** pass `pathfinder: new JsPathfinder()`, or `TravelSystem` is omitted and every travel-gated action silently no-ops (false "dormant"). See [open-questions.md](open-questions.md) for the JS-vs-WASM route caveat.
- **Leader-runaway / peer-interaction:** the old "one farmer runs away, field flat, peer layer inert" premise is **stale** (21-farmer radial field self-distributes; brief 59 fixed the peer-trade price bug + added `OFFER_CROP`). Full detail + the residual drama gaps in [open-questions.md](open-questions.md).

## Architecture milestones (no brief / cross-brief)

- **Seed-generated world (briefs 92 + 93, 2026-06-14):** the world is now **fully generated per seed** — rectangular islands (farms fixed-area/varied-aspect) placed by BSP, straight axis-aligned bridges with loops, ~60% land, runtime-varying via `WORLD_SEED`. Retired the radial-ring model + the brief-91 organic CA mask. Single funnel `generateWorld(seed)` + mutable `setActiveWorld` singleton; open-ocean boats (pass under bridges). Determinism: same `WORLD_SEED` → byte-identical (fast diff). Full detail in [world-generation.md](world-generation.md).
- **Client/server split (briefs 55–58):** the sim moved out of the browser into a Node server; the Vite app is a pure WebSocket client. Sim logic in `@farm/sim-core`; `@farm/server` hosts it; `npm run dev` runs both (Vite proxies `/sim`). Determinism held (WASM baseline). Deploy gained a pm2 + Caddy-WS phase (**dry-run-verified only — real VPS run pending**). Found along the way: JS≠WASM pathfinder routes (server uses WASM), and a fixed module-global `lastFacing` bug (now per-run `SnapshotSpriteState`). See [architecture.md](architecture.md), [decisions.md](decisions.md).
- **Post-corpus (no brief):** Canvas2D renderer (replaced WebGPU), in-house ECS (replaced miniplex), WASM pathfinding infra, the sim↔render snapshot/interpolate boundary, home screen, headless run-sim, offline world-preview, README, **Pip** + interaction systems, and the **160×160 radial archipelago** (2026-06-09, since grown to 240×240 and **superseded 2026-06-14 by the seed-generated rect-island world** — briefs 92/93 above). A 2026-06-06 refactor split every >300-line file into module directories fronted by barrels — see [architecture.md](architecture.md) → *Module-directory convention*.

## Open gaps
See [open-questions.md](open-questions.md) for the live list.
