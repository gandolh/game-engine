# Atlas artifacts are regenerated by @tool/atlas-builder with LF line endings and are
# a declared turbo cache input for @farm/sim-core#test (engine brief 21). Pin the bytes
# (-text = no EOL conversion) so a test run on Windows autocrlf can't dirty the tree or
# wobble the warm cache.
games/farm/client/public/atlas/* -text
