/**
 * MateQuest M3 — run-layer balance constants (corpus/todos/2026-07-22-mathquest-M3-map-and-runs.md).
 */

/** A `"rest"` map node heals the warrior by this much (capped at `warriorMaxHp` — see
 * `sim-bootstrap.ts`'s `chooseNode`). Provisional/tunable, like the M3 enemy archetypes. */
export const REST_HEAL = 20;
